1) 核心控制流由main.cpp负责全局初始化、控制流调度及业务逻辑编排，不包含底层实现；2) 所有功能逻辑必须划分为独立工具模块位于src/modules/下，如loader、slat、memory_manager；3) 每个模块必须提供原子化的高内聚功能接口；4) 严禁模块间循环引用，必要时引入抽象层解耦；5) 工具模块必须保持完全无状态设计，禁止在模块命名空间或类中定义私有成员变量；6) 禁止使用static变量、全局变量或单例模式持有状态；7) 所有运行状态必须封装在context_t结构体中并由上层main.cpp创建和管理；8) 所有需要状态支持的函数必须通过参数显式传入context_t*指针；9) 模块间交互仅通过参数传递，不共享任何状态；10) 每个函数必须在定义处添加JSDoc标准注释，包含description、param、return、throws和example标签；11) 每个复杂业务逻辑块必须在实现前添加中文业务注释，详细说明业务背景、输入输出、核心规则和异常处理策略；12) 每个源文件开头必须包含文件头注释，注明文件名、功能描述、版权信息和修改日期；13) 类型和结构体命名采用snake_case_t格式如context_t；14) 函数命名采用snake_case格式如memory_allocate；15) 宏常量和枚举值采用UPPER_SNAKE_CASE格式如PAGE_SIZE_4K；16) 所有函数必须返回status_t枚举值，通过返回值传递错误信息，禁止使用异常；17) 调用任何函数后必须检查其返回值；18) 模块开发前需确认功能是否足够原子化以及接口设计是否符合无状态原则；19) 代码审查时检查src/modules/下是否存在任何static或全局变量；20) 检查模块间是否存在循环依赖；21) 确认所有导出函数都包含完整JSDoc注释；22) 确认复杂逻辑前都有中文业务说明；23) 确认main.cpp仅包含控制流编排而非底层实现；24) 确认错误处理完整覆盖所有可能失败路径；25) 提交前确保代码编译无警告；26) 确保单元测试覆盖核心功能；27) 确保文档与代码同步更新；28) 严禁在模块内定义static uint64_t g_loaded_vms[64]这类私有状态数组；29) 严禁编写超过50行且无注释说明的复杂函数；30) 所有模块状态必须通过context_t结构体显式传递，例如loader模块应定义context_t结构体包含loaded_vms数组，并通过load_vm(context_t* ctx, uint64_t vm_id)接口操作。